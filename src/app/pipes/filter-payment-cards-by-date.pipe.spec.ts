import {FilterPaymentCardsByDatePipe} from './filter-payment-cards-by-date.pipe'

describe('FilterPaymentCardsByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPaymentCardsByDatePipe()
    expect(pipe).toBeTruthy()
  })
})
